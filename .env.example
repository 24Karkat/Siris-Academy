# Server-side endpoint the /api/leads route forwards captured leads to.
# Leave empty in dev — leads are logged to the server console and kept in
# localStorage on the client. Set to the real SIRISHub ingest URL in prod.
SIRISHUB_ENDPOINT=
